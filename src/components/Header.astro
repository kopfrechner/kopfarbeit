---
import IconX from "@/assets/icons/IconX.svg";
import IconMoon from "@/assets/icons/IconMoon.svg";
import IconSearch from "@/assets/icons/IconSearch.svg";
import IconArchive from "@/assets/icons/IconArchive.svg";
import IconSunHigh from "@/assets/icons/IconSunHigh.svg";
import IconMenuDeep from "@/assets/icons/IconMenuDeep.svg";
import LinkButton from "./LinkButton.astro";
import { SITE } from "@/config";

const { pathname } = Astro.url;

// Remove trailing slash from current pathname if exists
const currentPath =
  pathname.endsWith("/") && pathname !== "/" ? pathname.slice(0, -1) : pathname;

const isActive = (path: string) => {
  const currentPathArray = currentPath.split("/").filter(p => p.trim());
  const pathArray = path.split("/").filter(p => p.trim());

  return currentPath === path || currentPathArray[0] === pathArray[0];
};
---

<script is:inline src="https://unpkg.com/typewriter-effect@latest/dist/core.js"
></script>

<a
  id="skip-to-content"
  href="#main-content"
  class="absolute start-16 -top-full z-50 bg-background px-3 py-2 text-accent backdrop-blur-lg transition-all focus:top-4"
>
  Skip to content
</a>

<header
  class="app-layout mt-8 flex flex-col items-center justify-between md:flex-row"
>
  <div
    id="top-nav-wrap"
    class:list={[
      "py-4 sm:py-6",
      "mb-4",
      "relative w-full border-neo bg-zest p-6 text-ink shadow-neo",
      "flex items-center justify-between",
    ]}
  >
    <a
      href="/"
      class="hover-neo py-1 text-xl leading-8 whitespace-nowrap text-ink transition-all duration-200 md:my-auto md:text-3xl md:leading-none"
    >
      <span class="font-heading tracking-tighter uppercase"
        ><span id="typewriter-title">&gt; {SITE.title}_</span></span
      >
    </a>
    <nav
      id="nav-menu"
      class="flex flex-col items-center md:ms-2 md:flex-row md:justify-end md:space-x-4 md:py-0"
    >
      <button
        id="menu-btn"
        class="focus-outline self-end border-neo bg-background p-2 shadow-neo md:hidden"
        aria-label="Open Menu"
        aria-expanded="false"
        aria-controls="menu-items"
      >
        <IconX id="close-icon" class="hidden" />
        <IconMenuDeep id="menu-icon" />
      </button>
      <ul
        id="menu-items"
        class:list={[
          "mt-4 grid w-full grid-cols-2 place-content-center gap-4",
          "[&>li>a]:block [&>li>a]:px-6 [&>li>a]:py-3 [&>li>a]:text-center [&>li>a]:font-bold [&>li>a]:tracking-widest [&>li>a]:text-ink [&>li>a]:uppercase [&>li>a]:hover:opacity-75 md:[&>li>a]:px-4 md:[&>li>a]:py-2",
          "hidden",
          "md:mt-0 md:flex md:w-auto md:gap-x-2 md:gap-y-0",
        ]}
      >
        <li>
          <a
            href="/posts"
            class:list={[
              "border-neo bg-background shadow-neo md:border-0 md:bg-transparent md:shadow-none",
              { "active-nav": isActive("/posts") },
            ]}
          >
            Posts
          </a>
        </li>
        <li>
          <a
            href="/tags"
            class:list={[
              "border-neo bg-background shadow-neo md:border-0 md:bg-transparent md:shadow-none",
              { "active-nav": isActive("/tags") },
            ]}
          >
            Tags
          </a>
        </li>
        <li>
          <a
            href="/about"
            class:list={[
              "border-neo bg-background shadow-neo md:border-0 md:bg-transparent md:shadow-none",
              { "active-nav": isActive("/about") },
            ]}
          >
            About
          </a>
        </li>
        {
          SITE.showArchives && (
            <li>
              <LinkButton
                href="/archives"
                class:list={[
                  "focus-outline flex justify-center p-3 text-ink hover:opacity-75 md:p-1",
                  {
                    "active-nav": isActive("/archives"),
                  },
                ]}
                title="Archives"
                aria-label="archives"
              >
                <IconArchive class="hidden md:inline-block" />
                <span class="text-center font-bold tracking-widest uppercase md:sr-only">
                  Archives
                </span>
              </LinkButton>
            </li>
          )
        }
        <li class="flex items-center justify-center">
          <LinkButton
            href="/search"
            class:list={[
              "focus-outline flex p-3 text-ink hover:opacity-75 md:p-1",
              {
                "underline decoration-4 underline-offset-8":
                  isActive("/search"),
              },
            ]}
            title="Search"
            aria-label="search"
          >
            <IconSearch />
            <span class="sr-only">Search</span>
          </LinkButton>
        </li>
        {
          SITE.lightAndDarkMode && (
            <li class="flex items-center justify-center">
              <button
                id="theme-btn"
                class="focus-outline relative size-10 border-2 border-ink bg-white text-ink shadow-[4px_4px_0px_rgba(0,0,0,1)] transition-all hover:translate-x-[-1px] hover:translate-y-[-1px] hover:shadow-[5px_5px_0px_rgba(0,0,0,1)]"
                title="Toggles light & dark"
                aria-label="auto"
                aria-live="polite"
              >
                <IconMoon class="absolute top-[50%] left-[50%] size-5 -translate-[50%] scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
                <IconSunHigh class="absolute top-[50%] left-[50%] size-5 -translate-[50%] scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
              </button>
            </li>
          )
        }
      </ul>
    </nav>
  </div>
</header>

<script>
  import { SITE } from "@/config";

  // Global declaration for the external Typewriter library loaded via CDN
  declare const Typewriter: any;

  function initNav() {
    const menuBtn = document.querySelector("#menu-btn");
    const menuItems = document.querySelector("#menu-items");
    const menuIcon = document.querySelector("#menu-icon");
    const closeIcon = document.querySelector("#close-icon");

    if (menuBtn && menuItems && menuIcon && closeIcon) {
      menuBtn.addEventListener("click", () => {
        const openMenu = menuBtn.getAttribute("aria-expanded") === "true";

        menuBtn.setAttribute("aria-expanded", openMenu ? "false" : "true");
        menuBtn.setAttribute(
          "aria-label",
          openMenu ? "Open Menu" : "Close Menu"
        );

        menuItems.classList.toggle("hidden");
        menuIcon.classList.toggle("hidden");
        closeIcon.classList.toggle("hidden");
      });
    }
  }

  function initTypewriter() {
    const container = document.querySelector("#typewriter-title");
    if (container && typeof Typewriter !== "undefined") {
      new Typewriter(container, {
        loop: false,
        cursor: "_",
        delay: 75,
      })
        .deleteAll(0)
        .typeString(`> ${SITE.title}`)
        .start();
    }
  }

  // Animate only on first entry
  initTypewriter();

  // Initialize nav on entry and on every view transition
  initNav();
  document.addEventListener("astro:after-swap", initNav);
</script>
