---
phase: 01-styling-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/pages/index.astro
  - public/images/bg-left.webp
  - public/images/bg-right.webp
  - public/images/noise.svg
autonomous: true
must_haves:
  truths:
    - "Site metadata title is 'soda-zitrone.dev'"
    - "Browser tab shows 'soda-zitrone.dev'"
    - "Legacy background images are removed"
  artifacts:
    - path: "src/config.ts"
      provides: "Site configuration"
      contains: "soda-zitrone.dev"
  key_links:
    - from: "src/config.ts"
      to: "src/layouts/Layout.astro"
      via: "SITE object import"
---

<objective>
Update site identity to "soda-zitrone.dev" and remove legacy background assets to prepare for the new dot-grid pattern.

Purpose: Shift branding from "KopfarbeIT" to "Soda Zitrone" and clear out old assets.
Output: Updated config/pages and cleaner public directory.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-styling-baseline/01-CONTEXT.md
@src/config.ts
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Site Identity</name>
  <files>src/config.ts, src/pages/index.astro</files>
  <action>
    1. Update `src/config.ts`:
       - Set `website` to "https://soda-zitrone.dev"
       - Set `title` to "soda-zitrone.dev" (or "Soda Zitrone")
       - Update `desc` to reflect the new brand (keep it generic if not specified, but remove "KopfarbeIT").
    2. Update `src/pages/index.astro`:
       - Replace "Welcome to KopfarbeIT" with "Welcome to soda-zitrone.dev" (or relevant header).
  </action>
  <verify>
    grep "soda-zitrone" src/config.ts
    grep "KopfarbeIT" src/config.ts && echo "Found legacy branding" || echo "Clean"
  </verify>
  <done>
    `src/config.ts` reflects new domain and title.
    Homepage heading is updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Purge Legacy Assets</name>
  <files>public/images/bg-left.webp, public/images/bg-right.webp, public/images/noise.svg</files>
  <action>
    Delete the following files which are replaced by CSS patterns:
    - `public/images/bg-left.webp`
    - `public/images/bg-right.webp`
    - `public/images/noise.svg`
    
    Ensure no other files are deleted.
  </action>
  <verify>
    ls public/images/bg-left.webp 2>/dev/null && echo "Failed" || echo "Deleted"
  </verify>
  <done>
    Legacy background images are removed from filesystem.
  </done>
</task>

</tasks>

<verification>
grep "soda-zitrone.dev" src/config.ts
test ! -f public/images/noise.svg
</verification>

<success_criteria>
- Site title and URL are updated in config.
- Old background assets are deleted.
</success_criteria>

<output>
After completion, create `.planning/phases/01-styling-baseline/01-01-SUMMARY.md`
</output>
