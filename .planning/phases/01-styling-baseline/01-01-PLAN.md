---
phase: 01-styling-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/pages/index.astro
  - src/components/Header.astro
  - README.md
  - GEMINI.md
  - src/layouts/Layout.astro
  - public/images/bg-left.webp
  - public/images/bg-right.webp
  - public/images/noise.svg
autonomous: true
must_haves:
  truths:
    - "Site identity is updated to 'soda-zitrone.dev' / 'Soda Zitrone' globally"
    - "Typewriter effect in Header shows 'Soda Zitrone'"
    - "Legacy background images are removed from public/ and Layout.astro"
  artifacts:
    - path: "src/config.ts"
      provides: "Site configuration"
      contains: "soda-zitrone.dev"
    - path: "src/components/Header.astro"
      provides: "Site header"
      contains: "Soda Zitrone"
  key_links:
    - from: "src/config.ts"
      to: "src/layouts/Layout.astro"
      via: "SITE object import"
---

<objective>
Update site identity to "soda-zitrone.dev" / "Soda Zitrone" across configuration, UI, and documentation, and remove legacy background assets and references.

Purpose: Shift branding from "KopfarbeIT" to "Soda Zitrone" and clear out old assets to prevent broken image references.
Output: Updated identity in config/components/docs and cleaner public/layout files.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-styling-baseline/01-CONTEXT.md
@src/config.ts
@src/pages/index.astro
@src/components/Header.astro
@src/layouts/Layout.astro
@README.md
@GEMINI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global Identity Update</name>
  <files>src/config.ts, src/pages/index.astro, src/components/Header.astro, README.md, GEMINI.md</files>
  <action>
    Update all instances of "KopfarbeIT" and "Kopfarbeit" to "soda-zitrone.dev" or "Soda Zitrone":
    1. `src/config.ts`:
       - `website`: "https://soda-zitrone.dev"
       - `title`: "soda-zitrone.dev"
    2. `src/pages/index.astro`: Update Welcome header to "Welcome to soda-zitrone.dev".
    3. `src/components/Header.astro`: Update `initTypewriter` to use `.typeString("Soda Zitrone")`.
    4. `README.md` & `GEMINI.md`: Update headers and descriptions to reflect the new brand "Soda Zitrone".
  </action>
  <verify>
    grep -E "soda-zitrone|Soda Zitrone" src/config.ts src/components/Header.astro || exit 1
    grep -E "KopfarbeIT|Kopfarbeit" src/config.ts src/components/Header.astro README.md GEMINI.md && exit 1 || echo "Identity updated"
  </verify>
  <done>
    New domain and name are applied across the codebase and documentation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Purge Legacy Assets & References</name>
  <files>public/images/bg-left.webp, public/images/bg-right.webp, public/images/noise.svg, src/layouts/Layout.astro</files>
  <action>
    Remove legacy background implementation:
    1. Delete the following files:
       - `public/images/bg-left.webp`
       - `public/images/bg-right.webp`
       - `public/images/noise.svg`
    2. Update `src/layouts/Layout.astro`:
       - Remove the `<!-- Background Layers -->` div and all its contents (the images).
       - Keep the `body` class `paper-texture` for now (it will be updated in Plan 03).
  </action>
  <verify>
    ls public/images/bg-left.webp 2>/dev/null && exit 1 || echo "Files deleted"
    grep "Background Layers" src/layouts/Layout.astro && exit 1 || echo "References removed"
  </verify>
  <done>
    Legacy assets are deleted and no longer referenced in Layout.astro.
  </done>
</task>

</tasks>

<verification>
grep "soda-zitrone.dev" src/config.ts || exit 1
grep "Soda Zitrone" src/components/Header.astro || exit 1
test ! -f public/images/noise.svg || exit 1
grep -q "Background Layers" src/layouts/Layout.astro && exit 1 || exit 0
</verification>

<success_criteria>
- Site title and URL are updated in config.
- Header typewriter script uses new name.
- Documentation reflects new identity.
- Old background assets and their HTML tags are removed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-styling-baseline/01-01-SUMMARY.md`
</output>
