---
phase: 01-styling-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/styles/typography.css
autonomous: true
must_haves:
  truths:
    - "CSS variables for Zest, Fizz, Ink are defined"
    - "Archivo Black is applied to headings"
    - "JetBrains Mono is applied to body"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Neobrutalist theme variables"
    - path: "src/styles/typography.css"
      provides: "Typography definitions"
  key_links:
    - from: "src/styles/global.css"
      to: "Tailwind @theme"
      via: "inline theme definition"
---

<objective>
Implement the core neobrutalist CSS variables and integrate the required typography (Archivo Black & JetBrains Mono).

Purpose: Establish the visual foundation for the neobrutalist aesthetic.
Output: Updated global CSS and typography styles.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-styling-baseline/01-CONTEXT.md
@src/styles/global.css
@src/styles/typography.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typography Integration</name>
  <files>src/styles/global.css, src/styles/typography.css</files>
  <action>
    1. Add Google Font imports to `src/styles/global.css`:
       - `Archivo Black` (Headings)
       - `JetBrains Mono` (Body)
    2. Update `src/styles/typography.css`:
       - Apply `Archivo Black` to `h1, h2, h3, h4, h5, h6` and brand elements.
       - Style headings: `text-transform: uppercase`, `letter-spacing: -1px`.
    3. Update `body` font in `global.css` (or via Tailwind @theme) to use `JetBrains Mono`.
  </action>
  <verify>
    grep -E "@import.*Archivo Black|@import.*JetBrains Mono" src/styles/global.css || exit 1
    grep "font-family:.*Archivo Black" src/styles/typography.css || exit 1
  </verify>
  <done>
    New fonts are loaded and applied to global elements.
  </done>
</task>

<task type="auto">
  <name>Task 2: Neobrutalist CSS Variables</name>
  <files>src/styles/global.css</files>
  <action>
    Update `:root`, `html[data-theme="light"]`, and `html[data-theme="dark"]` in `src/styles/global.css`:
    
    Variables to set (using values from CONTEXT.md):
    - `--zest`: `#DAFF00`
    - `--fizz`: `#00F0FF` (Light) / `#FF00EA` (Dark)
    - `--ink`: `#000000`
    - `--background`: `#FFFDF5` (Light) / `#121212` (Dark)
    - `--foreground`: `#1A1A1A` (Light) / `#DAFF00` (Dark)
    - `--card-bg`: `#FFFFFF` (Light) / `#252525` (Dark)
    - `--border-width`: `3px`
    - `--shadow-color`: `#000000` (Light) / `#FFFFFF` (Dark)
    - `--shadow-offset`: `6px 6px 0px`

    Map these variables to Tailwind `@theme inline` as needed (updating existing `--color-accent`, etc.).
  </action>
  <verify>
    grep "--zest" src/styles/global.css || exit 1
    grep "html\[data-theme=\"dark\"\]" src/styles/global.css || exit 1
  </verify>
  <done>
    All core neobrutalist variables are defined and theme-aware.
  </done>
</task>

</tasks>

<verification>
grep -E "Archivo Black|JetBrains Mono" src/styles/global.css || exit 1
grep "--zest" src/styles/global.css || exit 1
</verification>

<success_criteria>
- Fonts Archivo Black and JetBrains Mono are loaded.
- Primary colors (Zest, Fizz) and shadows are correctly assigned to CSS variables.
</success_criteria>

<output>
After completion, create `.planning/phases/01-styling-baseline/01-02-SUMMARY.md`
</output>