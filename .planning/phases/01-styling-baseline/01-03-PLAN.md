---
phase: 01-styling-baseline
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/styles/global.css
  - src/layouts/Layout.astro
autonomous: false
must_haves:
  truths:
    - "Background shows a dot-grid pattern"
    - "Old background image tags are removed from Layout.astro"
    - "Elements have 3px black borders"
    - "Interactive elements lift on hover"
  artifacts:
    - path: "src/layouts/Layout.astro"
      provides: "Global page structure"
    - path: "src/styles/global.css"
      provides: "Final visual styles"
---

<objective>
Implement the global dot-grid background and finalize neobrutalist visual primitives (borders, shadows, and hover effects) across the site.

Purpose: Complete the visual baseline and remove legacy implementation details.
Output: A fully themed layout ready for component-level redesigns.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-styling-baseline/01-CONTEXT.md
@src/styles/global.css
@src/layouts/Layout.astro
# Need prior summaries for variable names and cleanup status
@.planning/phases/01-styling-baseline/01-01-SUMMARY.md
@.planning/phases/01-styling-baseline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dot-Grid Background Implementation</name>
  <files>src/styles/global.css, src/layouts/Layout.astro</files>
  <action>
    1. Update `src/layouts/Layout.astro`:
       - Remove the `<!-- Background Layers -->` div containing `/images/bg-left.webp` and `/images/bg-right.webp`.
       - Ensure `body` still has the `paper-texture` class (or renamed class).
    2. Update `src/styles/global.css`:
       - Replace the `.paper-texture` (and pseudo-element) background implementation with a `radial-gradient` dot grid:
         - Size: `1px` dots.
         - Grid: `40px 40px`.
         - Attachment: `fixed`.
       - Ensure dot color is high-contrast (Ink in Light mode, White in Dark mode).
  </action>
  <verify>
    Check `src/layouts/Layout.astro` for removed `img` tags.
    Check `src/styles/global.css` for `radial-gradient`.
  </verify>
  <done>
    Background is a clean, fixed dot pattern. Legacy blobs and noise are gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Baseline Neobrutalist Styles</name>
  <files>src/styles/global.css</files>
  <action>
    Implement global neobrutalist primitives in `src/styles/global.css`:
    1. **Borders:** Set a global `border: 3px solid var(--ink)` (or mapped Tailwind utility `border-neo`).
    2. **Shadows:** Update `shadow-neo` to use `6px 6px 0px var(--shadow-color)`.
    3. **Hover States:** Add a global utility or base style for neobrutalist interaction:
       - On hover: `translate: -2px -2px`, with an increased shadow (e.g., `8px 8px 0px var(--shadow-color)`).
    4. Ensure these are applied to standard buttons/cards as a baseline.
  </action>
  <verify>
    Check `@utility border-neo` and `@utility shadow-neo` in `global.css`.
    Verify hover transition styles.
  </verify>
  <done>
    Base elements (borders, shadows, hover) follow neobrutalist logic.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Visual Review: Baseline Styling</name>
  <what-built>The core visual foundation: colors, fonts, background, and neobrutalist primitives.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the site.
    2. Verify the background is a dot grid.
    3. Verify headings are in Archivo Black (uppercase).
    4. Verify the site name is "soda-zitrone.dev".
    5. Switch between Light and Dark mode:
       - Light: Creamy off-white bg, black dots, black shadows.
       - Dark: Dark bg, light dots, white shadows, Zest text.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Confirm dot grid is visible.
Confirm legacy assets are no longer referenced in Layout.astro.
Confirm high-contrast dark mode shifts.
</verification>

<success_criteria>
- No legacy background images referenced.
- Fixed dot-grid background is active.
- Borders and shadows use the defined neobrutalist offsets.
- Dark mode provides high contrast (Zest text on dark).
</success_criteria>

<output>
After completion, create `.planning/phases/01-styling-baseline/01-03-SUMMARY.md`
</output>
