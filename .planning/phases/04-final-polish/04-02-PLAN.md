---
phase: 04-final-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/layouts/Main.astro, src/layouts/AboutLayout.astro, src/layouts/PostDetails.astro, src/styles/typography.css]
autonomous: true

must_haves:
  truths:
    - "Main content containers have 0px border radius"
    - "About page containers have 0px border radius"
    - "Post detail containers have 0px border radius"
    - "Inline code and highlighted words have 0px border radius"
  artifacts:
    - path: "src/layouts/Main.astro"
      provides: "Sharp main layout"
    - path: "src/styles/typography.css"
      provides: "Sharp typography elements"
---

<objective>
Remove all remaining rounded corners from main layout files and global typography to achieve a 100% sharp neobrutalist look.

Purpose: Consistency. Neobrutalism relies on sharp, tactile edges.
Output: Rectangular containers and code blocks across the site.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/layouts/Main.astro
@src/layouts/AboutLayout.astro
@src/layouts/PostDetails.astro
@src/styles/typography.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Rounding from Layouts</name>
  <files>
    - src/layouts/Main.astro
    - src/layouts/AboutLayout.astro
    - src/layouts/PostDetails.astro
  </files>
  <action>
    Sweep through the layout files and remove all `rounded-*` classes from major containers:
    - `src/layouts/Main.astro`: Remove `rounded-md` from the `div` wrappers.
    - `src/layouts/AboutLayout.astro`: Remove `rounded-md` from the header and content wrappers.
    - `src/layouts/PostDetails.astro`: Remove `rounded-md` from the main container. Also remove `rounded-lg` from the `archived` notice block.
  </action>
  <verify>Check Homepage, About, and a Post page. Verify all main containers have sharp corners.</verify>
  <done>Layout containers are sharp and neobrutalist.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Rounding from Typography</name>
  <files>src/styles/typography.css</files>
  <action>
    Update `src/styles/typography.css`:
    - Remove `rounded` from the `code` selector.
    - Remove `rounded-sm` from the `.highlighted-word` selector.
  </action>
  <verify>Check a post with inline code and highlighted words. They should be sharp rectangles.</verify>
  <done>Typography elements follow the sharp aesthetic.</done>
</task>

</tasks>

<verification>
1. No `rounded-md` or `rounded-lg` remains in the specified layout files.
2. Inline code blocks are sharp.
</verification>

<success_criteria>
- Main layout containers have 0 rounding.
- Post headers and content areas have 0 rounding.
- Code blocks (inline) have 0 rounding.
</success_criteria>

<output>
After completion, create `.planning/phases/04-final-polish/04-02-SUMMARY.md`
</output>
