---
phase: 04-final-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/components/BackButton.astro, src/components/BackToTopButton.astro, src/components/Pagination.astro, src/layouts/PostDetails.astro, src/utils/transformers/fileName.js, src/pages/archives/index.astro]
autonomous: true

must_haves:
  truths:
    - "Buttons (Back, BackToTop) have 0px border radius"
    - "Pagination container has 0px border radius"
    - "Copy buttons have 0px border radius"
    - "Code block filename badges have 0px border radius"
  artifacts:
    - path: "src/components/BackButton.astro"
      provides: "Sharp Back button"
    - path: "src/utils/transformers/fileName.js"
      provides: "Sharp filename badges"
---

<objective>
Remove all rounded corners from secondary components and scripts, including buttons, pagination, and dynamic UI elements created by scripts.

Purpose: Eliminate the last remaining curves for total aesthetic consistency.
Output: Sharp buttons, sharp pagination, and sharp code block badges.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/BackButton.astro
@src/components/BackToTopButton.astro
@src/components/Pagination.astro
@src/layouts/PostDetails.astro
@src/utils/transformers/fileName.js
@src/pages/archives/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sharpen Components</name>
  <files>
    - src/components/BackButton.astro
    - src/components/BackToTopButton.astro
    - src/components/Pagination.astro
    - src/pages/archives/index.astro
  </files>
  <action>
    Remove `rounded-*` classes from:
    - `src/components/BackButton.astro`: Remove `rounded-md`.
    - `src/components/BackToTopButton.astro`: Change `rounded-full` and `rounded-md` to `rounded-none`.
    - `src/components/Pagination.astro`: Remove `rounded-md`.
    - `src/pages/archives/index.astro`: Remove `rounded-lg` from the archived notice.
  </action>
  <verify>Check post pages and list pages. Verify Back, BackToTop, and Pagination are all sharp.</verify>
  <done>Interactive components are sharp and neobrutalist.</done>
</task>

<task type="auto">
  <name>Task 2: Sharpen Scripted UI Elements</name>
  <files>
    - src/layouts/PostDetails.astro
    - src/utils/transformers/fileName.js
  </files>
  <action>
    Update scripts that create UI elements:
    - `src/layouts/PostDetails.astro`: In `attachCopyButtons`, remove `rounded` from the `copyButton.className`.
    - `src/utils/transformers/fileName.js`: 
        - Remove `rounded-tl-none` from code block class additions.
        - Change `rounded-t-md` and `rounded-md` to `rounded-none` in the filename badge classes.
        - **Keep** `rounded-full` for the green dot indicator (dots are part of the brand).
  </action>
  <verify>Check a post with code blocks. Verify the "Copy" button and filename badges have sharp corners.</verify>
  <done>Dynamic UI elements follow the sharp aesthetic.</done>
</task>

</tasks>

<verification>
1. All buttons are rectangular.
2. Pagination is rectangular.
3. Code block metadata (filename, copy button) is rectangular.
</verification>

<success_criteria>
- No `rounded-*` classes remain in the target components.
- Scripts create rectangular elements.
</success_criteria>

<output>
After completion, create `.planning/phases/04-final-polish/04-03-SUMMARY.md`
</output>
