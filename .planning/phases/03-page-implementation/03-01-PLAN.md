---
phase: 03-page-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Header.astro, src/components/Footer.astro]
autonomous: true

must_haves:
  truths:
    - "Header container has sharp edges (no rounding) and 3px black borders"
    - "Header title 'Soda Zitrone' is prominently displayed with its animated identity"
    - "Footer container has sharp edges and high-contrast neobrutalist styling"
    - "Navigation links feel chunky and high-contrast"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Structural header with new branding"
    - path: "src/components/Footer.astro"
      provides: "Structural footer with neobrutalist styling"
  key_links:
    - from: "src/components/Header.astro"
      to: "src/styles/global.css"
      via: "Tailwind classes (border-neo, shadow-neo, font-heading)"
---

<objective>
Redesign the site's structural components (Header and Footer) to fully embrace the "Soda Zitrone" neobrutalist identity.

Purpose: Structural elements provide the frame for the entire site. They must consistently communicate the brand's tactile, high-contrast aesthetic.
Output: Updated Header.astro and Footer.astro components.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Header.astro
@src/components/Footer.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Header.astro</name>
  <files>src/components/Header.astro</files>
  <action>
    - Remove all `rounded-*` classes from the `#top-nav-wrap` container.
    - Ensure the container uses `bg-card-bg`, `border-neo`, and `shadow-neo`.
    - Update navigation links (`ul#menu-items a`):
      - Increase padding for a "chunky" feel.
      - Apply `font-bold` and ensure high contrast.
      - Use `hover:text-accent` (Zest) for hover states.
    - Identity Polish:
      - Ensure the typewriter effect continues to type "Soda Zitrone".
      - Apply `font-heading` (Archivo Black) to the brand title.
      - Add a `hover-neo` effect to the logo/title link to make it "tactile".
  </action>
  <verify>
    - Header container is sharp-edged.
    - Hovering the brand title causes it to "jump" (translate).
    - Navigation links have generous hit areas and bold styling.
  </verify>
  <done>Header matches the neobrutalist brand spec.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Footer.astro</name>
  <files>src/components/Footer.astro</files>
  <action>
    - Remove all `rounded-*` classes from the footer container.
    - Apply `border-neo` and `shadow-neo`.
    - Increase visual impact:
      - Use a high-contrast background (e.g., `bg-ink text-background` or `bg-card-bg` with very thick borders).
      - Ensure social icons and links use `hover:text-accent` (Zest).
    - Ensure the footer is centered and maintains its "app-layout" width.
  </action>
  <verify>
    - Footer is sharp-edged and high-contrast.
    - All links in footer are functional and styled consistently.
  </verify>
  <done>Footer is consistent with the neobrutalist theme.</done>
</task>

</tasks>

<verification>
- Check Header and Footer in both Light and Dark modes.
- Verify that no rounded corners remain in structural elements.
- Confirm "Soda Zitrone" identity is clear and tactile.
</verification>

<success_criteria>
- Header and Footer have 3px solid borders and 6px hard shadows.
- No `rounded-*` classes remain in Header.astro or Footer.astro.
- Header title is in Archivo Black and has tactile hover state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-implementation/03-01-SUMMARY.md`
</output>
