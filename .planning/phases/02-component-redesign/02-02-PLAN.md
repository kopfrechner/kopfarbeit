---
phase: 02-component-redesign
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/pixel/Lemon.astro
  - src/components/pixel/Bottle.astro
  - src/components/pixel/Leaf.astro
autonomous: true
must_haves:
  truths:
    - "Lemon component renders a pixel-art lemon SVG"
    - "Bottle component renders a pixel-art bottle SVG"
    - "Leaf component renders a pixel-art leaf SVG"
  artifacts:
    - path: "src/components/pixel/Lemon.astro"
    - path: "src/components/pixel/Bottle.astro"
    - path: "src/components/pixel/Leaf.astro"
  key_links: []
---

<objective>
Implement SVG-based pixel art components for branding elements.
These will be used as decorations in cards, headers, and backgrounds.

Purpose: Add unique "soda zitrone" visual flair.
Output: Three reusable Astro components rendering SVG pixel art.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lemon pixel art component</name>
  <files>src/components/pixel/Lemon.astro</files>
  <action>
    Create `src/components/pixel/Lemon.astro`.
    Render an SVG with `viewBox="0 0 16 16"` (or similar small grid).
    Use `<rect>` elements to draw a pixelated lemon.
    Use `fill="var(--zest)"` for the body and `var(--ink)` for the outline.
    Ensure it accepts `class` props for sizing/positioning.
  </action>
  <verify>
    Create a test page or insert into index temporarily.
    Verify it looks like a lemon and scales with CSS.
  </verify>
  <done>Lemon component exists and renders.</done>
</task>

<task type="auto">
  <name>Task 2: Create Bottle pixel art component</name>
  <files>src/components/pixel/Bottle.astro</files>
  <action>
    Create `src/components/pixel/Bottle.astro`.
    Render an SVG representing a soda bottle.
    Use `var(--fizz)` or `var(--foreground)` colors.
    Pixel art style (blocky rects).
  </action>
  <verify>
    Verify visual appearance.
  </verify>
  <done>Bottle component exists.</done>
</task>

<task type="auto">
  <name>Task 3: Create Leaf pixel art component</name>
  <files>src/components/pixel/Leaf.astro</files>
  <action>
    Create `src/components/pixel/Leaf.astro`.
    Render an SVG representing a mint leaf or lemon leaf.
    Use green or accent colors (maybe hardcoded green if not in vars, or use Zest with opacity/mix).
    Actually, use `var(--ink)` for outline and `var(--zest)` or a new green if needed.
    (Self-correction: If no green variable exists, use `currentColor` or define a local color, or just Zest/Ink).
    Let's use `currentColor` for flexibility, or `var(--zest)` for brand consistency.
  </action>
  <verify>
    Verify visual appearance.
  </verify>
  <done>Leaf component exists.</done>
</task>

</tasks>

<verification>
- Components render scalable SVGs.
- Pixel art aesthetic (sharp edges, grid-based).
- Colors respect theme variables (Zest, Fizz, Ink).
</verification>

<success_criteria>
- High-quality pixel art assets available for use.
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-redesign/02-02-SUMMARY.md`
</output>
