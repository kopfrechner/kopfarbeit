---
phase: 02-component-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LinkButton.astro
  - src/components/Tag.astro
  - src/components/Card.astro
autonomous: true
must_haves:
  truths:
    - "Cards have hard 90-degree corners (no rounding)"
    - "Cards have 3px solid borders and 6px hard shadows"
    - "Tags look like buttons and 'jump' on hover"
    - "LinkButtons with variant='button' jump on hover"
  artifacts:
    - path: "src/components/LinkButton.astro"
      provides: "Button variant logic"
    - path: "src/components/Tag.astro"
      provides: "Neobrutalist tag styling"
    - path: "src/components/Card.astro"
      provides: "Hard-edged card design"
  key_links:
    - from: "src/components/Tag.astro"
      to: "src/components/LinkButton.astro"
      via: "variant prop"
---

<objective>
Transform core UI primitives (Cards, Buttons, Tags) into tactile, high-contrast neobrutalist elements.
Ensure consistent "hover jump" behavior for interactive elements.

Purpose: Establish the tactile feel of the interface before building complex pages.
Output: Updated Card, Tag, and LinkButton components matching the neobrutalist spec.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@src/styles/global.css
@src/components/Card.astro
@src/components/LinkButton.astro
@src/components/Tag.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LinkButton variants</name>
  <files>src/components/LinkButton.astro</files>
  <action>
    Update LinkButton.astro to accept a `variant` prop ("link" | "button", default "link").
    
    If `variant="button"`:
    - Apply `border-neo shadow-neo hover-neo bg-card-bg text-foreground px-4 py-1 no-underline`
    - Ensure it retains accessibility features.
    
    If `variant="link"`:
    - Retain existing behavior (dashed underline, hover effects).
    
    Update the `class:list` logic to conditionally apply these utility classes based on the variant.
  </action>
  <verify>
    Check existing links (e.g., in footer) still look like links.
    Create a temporary test page with `<LinkButton variant="button">Test</LinkButton>` and verify it looks like a button.
  </verify>
  <done>LinkButton supports visual variants.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Tag component</name>
  <files>src/components/Tag.astro</files>
  <action>
    Update Tag.astro to use `LinkButton` with `variant="button"`.
    - Adjust sizing classes if necessary to fit the new button style.
    - Ensure the hash icon aligns correctly within the button.
    - Remove any conflicting classes from the `LinkButton` usage in Tag.
  </action>
  <verify>
    Inspect tags on the /tags page or in a post list.
    Verify they have borders, shadows, and jump on hover.
  </verify>
  <done>Tags are tactile, button-like elements.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign Card component</name>
  <files>src/components/Card.astro</files>
  <action>
    Update Card.astro:
    - Remove all `rounded-*` classes to enforce sharp 90-degree corners.
    - Ensure `shadow-neo` and `border-neo` are present.
    - Verify `bg-card-bg` is used.
    - Ensure the hover effect `hover-neo` is active on the card container (so the whole card lifts).
    - Check if internal padding needs adjustment for the thick border.
  </action>
  <verify>
    Inspect cards on the homepage.
    Verify 3px border, 6px shadow, no rounded corners.
    Verify hover jump effect.
  </verify>
  <done>Cards match the strict neobrutalist spec.</done>
</task>

</tasks>

<verification>
- Cards are sharp-edged (no rounding).
- Tags and Buttons "jump" up-left on hover.
- Shadow offsets are 6px (hard).
- Borders are 3px (solid).
</verification>

<success_criteria>
- Visual consistency with neobrutalist theme.
- Interactive feedback (hover states) is "tactile".
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-redesign/02-01-SUMMARY.md`
</output>
