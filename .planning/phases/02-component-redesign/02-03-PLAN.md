---
phase: 02-component-redesign
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/components/Card.astro
  - src/pages/index.astro
autonomous: true
must_haves:
  truths:
    - "Buttons and Tags 'jump' on hover"
    - "Cards are sharp and tactile"
    - "Pixel art decorations appear in the UI (e.g. on Cards or Headings)"
  artifacts:
    - path: "src/components/Card.astro"
  key_links: []
---

<objective>
Integrate the new assets and polished styles into the main interface.
Verify the "Freshness" goal.

Purpose: Bring the redesign together and ensure components work in context.
Output: Polished UI with decorations.
</objective>

<execution_context>
@/Users/christophkopf/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/christophkopf/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/components/Card.astro
@src/components/pixel/Lemon.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pixel art decoration to Cards</name>
  <files>src/components/Card.astro</files>
  <action>
    Import `Lemon` or `Leaf`.
    Add a decorative element to `Card.astro`.
    Example: A small Lemon icon in the top-right corner, or next to the title.
    Use `absolute` positioning or flex layout.
    Ensure it doesn't obscure content.
    Make it optional via props? (e.g. `decorate?: boolean`).
    For now, maybe just add it subtly to all cards to test "Freshness".
  </action>
  <verify>
    Check homepage. Cards should have a zest of lemon.
  </verify>
  <done>Cards have brand personality.</done>
</task>

<task type="auto">
  <name>Task 2: Update Index page LinkButtons</name>
  <files>src/pages/index.astro</files>
  <action>
    Check usage of `LinkButton` in `src/pages/index.astro`.
    For "All Posts", ensure it uses `variant="button"`.
    (It currently uses manual classes. Refactor to use the new prop).
    Remove the manual `bg-card-bg shadow-neo border-neo` classes from the instance and rely on the component.
  </action>
  <verify>
    "All Posts" button looks correct and behaves consistent with Tags.
  </verify>
  <done>Index page code is cleaner and consistent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Neobrutalist Cards (sharp, thick borders)
    - Tactile Buttons & Tags (hover jump)
    - Pixel Art Assets (Lemon, Bottle, Leaf)
    - Integrated decorations
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Hover over a Card. Does it lift? Is it sharp?
    3. Hover over a Tag. Does it jump?
    4. Do you see the Lemon/Leaf decorations?
    5. Is the "Freshness" vibe achieved?
  </how-to-verify>
  <resume-signal>Type "approved" if it feels fresh.</resume-signal>
</task>

</tasks>

<verification>
- Design matches the "soda-zitrone" aesthetic.
- Components are reusable and robust.
</verification>

<success_criteria>
- All Phase 2 requirements met.
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-redesign/02-03-SUMMARY.md`
</output>
